import styled from 'styled-components';

const LogoContainer = styled.div`
    height: 86px;

    img {
        max-width: 100%;
        height: 100%;
    }
`;

export default LogoContainer;
